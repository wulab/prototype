<%= form_for @micropost do |f| %>
<div class="fields">
  <%= error_messages_for(@micropost, :content) %>
  <%= f.text_area :content, :cols => 20, :rows => 4 %>
</div>
<div class="actions">
  <%= f.submit "Submit" %>
</div>
<% end %>
